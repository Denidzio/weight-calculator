ls:
  .dir: regex:([.a-z0-9]*)([-.][a-z0-9]+)*
  .ts: regex:([.a-z0-9]*)([-.][a-z0-9]+)*
  .tsx: regex:([.a-z0-9]*)([-.][a-z0-9]+)*

ignore:
  - node_modules
  - android
  - ios
  - __tests__
  - .git
